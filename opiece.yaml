jwt:
  secret_key: abc
  expire_time: 604800
  buffer_time: 86400

db:
  type: postgres

postgres:
  db_host: localhost
  db_port: 5432
  db_name: opiecedb
  db_username: opiece
  db_password: postgres
  ssl_mode: disable
  timezone: Asia/Shanghai