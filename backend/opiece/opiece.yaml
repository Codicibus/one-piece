jwt:
  secret_key: abc
  expire_time: 604800
  buffer_time: 86400

gin:
  mode: debug
  port: 8081

cors:
  origins: ["https://123mtr.top/"]

db:
  type: postgres

postgres:
  db_host: localhost-1
  db_port: 54321
  db_name: opiecedb
  db_username: opiece
  db_password: postgres
  ssl_mode: disable
  timezone: Asia/Shanghai
